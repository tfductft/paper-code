# requirements.txt — deam_texture_servo
# Python >= 3.12
#
# NOTE: Install PyTorch with the correct CUDA/CPU build from the official instructions:
#   https://pytorch.org/get-started/locally/
# Examples:
#   # CUDA 12.4 (Linux, pip):
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu124
#   # CPU only:
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cpu

torch==2.4.0
torchvision==0.19.0

# ViT backbone + features_only
timm==1.0.8

# FLOPs/Params profiling (used by scripts/print_summary.py)
thop==0.1.1.post2209072238

# Core scientific stack
numpy>=1.26
pillow>=10.0.0

# --- Optional (uncomment if you need them) ---
# pytest>=7.4           # unit tests
# torchsummary>=1.5.1   # layer-wise summary (if you use it)
# einops>=0.6           # only if you add code that uses einops